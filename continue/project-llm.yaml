# Continue IDE model config (copy to .continue/config.yaml or merge as needed).
# Use apiBase http://127.0.0.1:8000/v1 for the backend directly, or :8001/v1 for hmac-proxy when HMAC is enforced.
name: Project LLM (local proxy)
version: 1.0.0
schema: v1

models:
  - name: GPT-5 (project proxy)
    provider: openai
    model: gpt-5
    useResponsesApi: false
    apiBase: http://127.0.0.1:8001/v1
    apiKey: REPLACE_WITH_YOUR_SK_PROJ_KEY
    roles:
      - chat
      - edit
      - apply
